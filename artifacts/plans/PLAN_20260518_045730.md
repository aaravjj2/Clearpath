# Improvement Plan — 2026-05-18T04:57:30Z (Loop 2)

## Summary of Findings

Carrying backlog from Loop 1. `requirements.txt` does not include `slowapi`, so rate limiting
must either use slowapi (add to requirements) or a custom FastAPI middleware (no new dep).
Missing e2e test files: `02_upload_pdf.spec.ts`, `04_streaming_analysis.spec.ts`.
`ProviderRegistry.complete()` has no retry — a single transient network blip fails the whole request.
Frontend upload does no size checking before the HTTP request is sent.

## Prioritized Improvements

### P1 (this loop)

| ID | Title | Files | Acceptance Criteria | Complexity |
|----|-------|-------|---------------------|------------|
| IMP-005 | Retry with exponential backoff in ProviderRegistry | `backend/services/providers.py` | Retries up to 3 times on transient errors before propagating | M |
| IMP-006 | Simple rate-limit middleware (no new dep) | `backend/main.py` + new `backend/middleware/rate_limit.py` | >60 req/min from same IP gets HTTP 429 | M |
| IMP-007 | Add missing e2e test files | `frontend/tests/e2e/02_upload_pdf.spec.ts`, `04_streaming_analysis.spec.ts` | Files exist with meaningful tests | S |
| IMP-009 | Frontend file-size validation before upload | `frontend/components/UploadZone.tsx` | Files > 20 MB show inline error, no HTTP request made | S |

**Items selected this loop:** IMP-005, IMP-006, IMP-007, IMP-009
