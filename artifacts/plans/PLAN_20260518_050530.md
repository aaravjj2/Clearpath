# Improvement Plan — 2026-05-18T05:05:30Z (Loop 4)

## Summary of Findings

ClauseCard already has expand/collapse (IMP-017 done in prior work, not by this loop).
New findings:
- `RiskGauge` SVG has no `role="img"` or `aria-label` — screen readers can't describe it.
- Paste textarea in `page.tsx` has no character count feedback near the 500k limit.
- `ClauseCard` does not render `key_terms` even though the data is present.
- Structured logging: request-ID middleware adds header, but no JSON log line per request.
- `backend/routers/documents.py` stream endpoint should set `Content-Type: text/event-stream` explicitly in `StreamingResponse` headers.

## Prioritized Improvements

### P1 (this loop)

| ID | Title | Files | Acceptance Criteria | Complexity |
|----|-------|-------|---------------------|------------|
| IMP-016 | JSON structured logging middleware | `backend/middleware/logging.py`, `backend/main.py` | Every request logs JSON with method, path, status, duration, request_id | S |
| IMP-018 | Character counter in paste textarea | `frontend/app/page.tsx` | Counter shows X/500,000; turns red at 90% | S |
| IMP-019 | ARIA on RiskGauge SVG | `frontend/components/RiskGauge.tsx` | role="img", aria-label="Risk score: N/100", aria-valuenow | S |
| IMP-020 | Render key_terms in ClauseCard | `frontend/components/ClauseCard.tsx` | Key terms shown as pill badges in expanded section | S |

**Items selected:** IMP-016, IMP-018, IMP-019, IMP-020
