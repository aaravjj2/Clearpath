# Improvement Plan — 2026-05-18T05:01:30Z (Loop 3)

## Summary of Findings

Backlog items: IMP-008 (request-ID), IMP-010 (aria labels), IMP-013 (loading skeleton).
Additional analysis:
- `backend/services/ai.py` `_extract_json`: if the model returns text with stray characters before/after the
  JSON block, `json.loads` fails silently (500). Need a more robust extractor.
- `frontend/components/ChatPanel.tsx`: send button lacks `aria-label`; input has no `id`/`aria-label`.
- No loading skeleton for the streaming clause list — just shows empty space until first clause arrives.
- `backend/routers/documents.py`: `StreamingResponse` has no `X-Request-ID` header.

## Prioritized Improvements

### P1 (this loop)

| ID | Title | Files | Acceptance Criteria | Complexity |
|----|-------|-------|---------------------|------------|
| IMP-008 | Request-ID middleware | `backend/middleware/request_id.py`, `backend/main.py` | Every response has `X-Request-ID` UUID header | S |
| IMP-010 | ARIA labels on ChatPanel | `frontend/components/ChatPanel.tsx` | Send button and input have aria-label; axe wcag2a passes | S |
| IMP-013 | Loading skeleton for clause list | `frontend/components/StreamingLoader.tsx` | Pulse-animated skeleton cards shown while streaming | S |
| IMP-015 | Robust JSON extraction in ai.py | `backend/services/ai.py` | Handles extra whitespace, nested code fences, BOM characters | S |

**Items selected:** IMP-008, IMP-010, IMP-013, IMP-015
