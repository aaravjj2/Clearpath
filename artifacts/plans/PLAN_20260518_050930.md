# Improvement Plan — 2026-05-18T05:09:30Z (Loop 5)

## Summary of Findings

- RedFlagPanel shows flags in arrival order; sorting by severity (3→1) is more useful.
- Health endpoint returns minimal info; providers-available status would help diagnose issues.
- Paste textarea has no keyboard shortcut (Ctrl+Enter) to submit — friction for power users.
- `backend/routers/documents.py` — the `StreamingResponse` should be wrapped in a helper
  that adds the X-Request-ID from `request.state` to SSE response headers.
- `ProviderRegistry` singleton is never reset across test runs, causing state leak in tests.

## Prioritized Improvements

### P1 (this loop)

| ID | Title | Files | Acceptance Criteria | Complexity |
|----|-------|-------|---------------------|------------|
| IMP-021 | Sort RedFlagPanel by severity desc | `frontend/components/RedFlagPanel.tsx` | Red flags ordered 3→2→1; data-testid added | S |
| IMP-022 | Detailed health endpoint | `backend/main.py` | `/health/detailed` returns version, environment, provider list | S |
| IMP-023 | Ctrl+Enter shortcut on paste textarea | `frontend/app/page.tsx` | Pressing Ctrl+Enter in paste area triggers analysis | S |
| IMP-024 | Reset registry between tests | `backend/test_api.py`, `backend/test_services.py` | Each test uses a fresh registry; no state bleed | S |

**Items selected:** IMP-021, IMP-022, IMP-023, IMP-024
