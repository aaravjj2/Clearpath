# Improvement Plan — 2026-05-18T04:52:00Z (Loop 1)

## Summary of Findings

Static analysis (manual code review, shell non-functional):

**Backend:**
- `backend/routers/chat.py` line 16: `answer_question` is `async` but called without `await` — returns a coroutine instead of a string. **This breaks chat entirely.**
- `backend/routers/documents.py`: No file-size limit on `upload_document`; large PDFs can cause OOM.
- `backend/services/ai.py` `_extract_json`: bare `json.loads` with no context in exception — silent failures map to 500 errors.
- `backend/services/vectorstore.py`: `query_document` calls `collection.query(n_results=n)` but if the collection has fewer docs than `n_results`, ChromaDB raises an error.
- No `backend/tests/` directory — tests are at repo root of backend, not under a `tests/` subfolder.

**Frontend:**
- `frontend/app/analyze/[docId]/page.tsx` line 33: `onError: (err) => console.error(err)` — errors are never shown to the user.
- `frontend/lib/api.ts` `uploadDocument`: no client-side file size check before the request.
- No `02_upload_pdf.spec.ts` or `04_streaming_analysis.spec.ts` test files exist.
- `frontend/components/ChatPanel.tsx`: no `aria-label` on send button or input.

**Prioritized Improvements:**

### P1 (this loop)

| ID | Title | Files | Acceptance Criteria | Complexity |
|----|-------|-------|---------------------|------------|
| IMP-001 | Fix missing `await` on `answer_question` in chat router | `backend/routers/chat.py` | `test_api.py::test_chat_not_indexed` passes; manual chat returns a string not `<coroutine>` | S |
| IMP-002 | File-size validation on upload endpoint | `backend/routers/documents.py` | POST with >20 MB text returns 413; existing upload tests still pass | S |
| IMP-003 | Show streaming errors in UI (not just console) | `frontend/app/analyze/[docId]/page.tsx` | Error state rendered when stream returns error event | S |
| IMP-004 | Guard ChromaDB n_results against collection size | `backend/services/vectorstore.py` | No exception when querying collection with 1 doc and n_results=3 | S |

### P2 (next loops)
- IMP-005: Retry logic for AI provider calls (exponential backoff)
- IMP-006: Rate limiting (slowapi) on FastAPI
- IMP-007: Add 02_upload_pdf.spec.ts & 04_streaming_analysis.spec.ts
- IMP-008: Request-ID middleware + structured logging
- IMP-009: Frontend file-size validation before upload
- IMP-010: Aria labels on chat input & send button

### P3 (future loops)
- IMP-011: Persistent ChromaDB (PersistentClient)
- IMP-012: JWT auth layer
- IMP-013: Loading skeletons in streaming view
- IMP-014: Chat history localStorage persistence
- IMP-015: Bundle analyzer + tree-shaking

**Items selected this loop:** IMP-001, IMP-002, IMP-003, IMP-004
