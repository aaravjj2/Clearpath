# Improvement Report — 2026-05-18T05:08:00Z (Loop 4)

## Improvements Implemented

| ID | Title | Files Changed | Description |
|----|-------|--------------|-------------|
| IMP-016 | Structured JSON request logging | `backend/middleware/logging.py`, `backend/main.py` | One JSON log line per request with ts, request_id, method, path, status, duration_ms, ip |
| IMP-018 | Character counter in paste textarea | `frontend/app/page.tsx` | Live count (X/500,000); turns amber at 400k, red at 450k; `aria-live="polite"` |
| IMP-019 | ARIA on RiskGauge SVG | `frontend/components/RiskGauge.tsx` | `role="img"`, `<title>`, `aria-label` on SVG and score text; fixed SVG to use `<circle>` with proper `strokeDasharray` |
| IMP-020 | Key terms in ClauseCard | `frontend/components/ClauseCard.tsx` | Pill badges for each key term shown in expanded section |

## Before / After

**IMP-016:** Requests were invisible in logs. Now every request emits a structured JSON record.

**IMP-018:** Users had no feedback on how close they were to the 500k limit. Now they see a live counter that warns in red.

**IMP-019:** RiskGauge was SVG with no text alternative. Now screen readers hear "Risk score: 72/100 — High risk".

**IMP-020:** `key_terms` array was returned by the AI but never shown in the UI. Now displayed as pill badges in ClauseCard expanded view.

## Next Loop Preview

1. IMP-021: RedFlagPanel — add data-testid and sort by severity
2. IMP-022: Backend — add `GET /health/detailed` endpoint (version, env, providers available)
3. IMP-023: Frontend — keyboard shortcut Ctrl+Enter to submit from paste textarea
