# Improvement Report — 2026-05-18T05:04:00Z (Loop 3)

## Improvements Implemented

| ID | Title | Files Changed | Description |
|----|-------|--------------|-------------|
| IMP-008 | Request-ID middleware | `backend/middleware/request_id.py`, `backend/main.py` | Every response now has `X-Request-ID` UUID header; echoes client-provided ID |
| IMP-010 | ARIA labels on ChatPanel | `frontend/components/ChatPanel.tsx` | Added `aria-label`, `id`, `data-testid="chat-input"` to input; `aria-label="Send question"` to button; `aria-hidden` on icon |
| IMP-013 | Enhanced loading skeleton | `frontend/components/StreamingLoader.tsx` | `role="status"`, `aria-label`, staggered animation delay, key-terms skeleton row, configurable count prop |
| IMP-015 | Robust JSON extraction | `backend/services/ai.py` | Handles BOM, prose-wrapped JSON, code fences, trailing commas; 4 new unit tests added |

## Test Results

- Backend: 4 new tests in `test_services.py` for `_extract_json` variants
- Frontend E2E: No new tests this loop (existing tests cover aria via axe)

## Before / After

**IMP-008:** Responses now carry `X-Request-ID` for tracing across logs/client.

**IMP-010:** Screen readers can now navigate the chat interface meaningfully; axe wcag2a should have 0 serious violations in ChatPanel.

**IMP-013:** Skeleton has `role="status"` so screen readers announce loading state; staggered animation feels more natural.

**IMP-015:** Model responses with extra prose, BOM, or trailing commas no longer cause 500 errors during analysis.

## Next Loop Preview

1. IMP-016: Structured logging with request IDs (log every request as JSON)
2. IMP-017: `ClauseCard` component — add expand/collapse for long original text
3. IMP-018: Paste text area — show character count and warn at 450k/500k
